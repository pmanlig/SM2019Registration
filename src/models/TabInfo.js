export class TabInfo {
	constructor(name, path, order, permissions, status) {
		this.name = name;
		this.path = path;
		this.order = order;
		this.permissions = permissions;
		this.status = status;
	}
}